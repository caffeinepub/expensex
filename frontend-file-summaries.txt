{
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions including accounts, transactions, categories, budgets, settings, and profile. Contains useTransferMoney mutation and all CRUD operations with proper cache invalidation.",
  "frontend/src/components/LoginScreen.tsx": "Login screen component with Internet Identity integration, feature highlights, and branding. Replaced deprecated `flex-shrink-0` with `shrink-0`.",
  "frontend/src/pages/HomePage.tsx": "Dashboard home page showing total balance, monthly income/expense, account cards, budget overview, and recent transactions. Replaced deprecated `flex-shrink-0` with `shrink-0` and added footer with caffeine.ai attribution.",
  "frontend/src/pages/RecordsPage.tsx": "Transactions list page with search, filter panel (category/account/sort), and delete functionality. Fixed `useFilterTransactions` call to pass `undefined` instead of `null` for optional `startTime`, `endTime`, `category`, and `accountId` params to match updated hook types.",
  "frontend/src/App.tsx": "Root application component that handles authentication state (login screen vs authenticated app), profile setup modal (shown when name is empty or profile is null), settings/theme providers, and page routing via AppLayout. Fixed TypeScript errors by using `== null` (covers both `null` and `undefined`) instead of strict `!== null` checks on `userProfile`.",
  "frontend/src/pages/AnalysisPage.tsx": "Analysis and reports page with summary cards (balance/income/expense), time filter (weekly/monthly/yearly), bar chart (income vs expense), pie chart (category-wise expenses), line chart (spending trend), and PDF export button.",
  "frontend/src/pages/SettingsPage.tsx": "Settings page with currency selector (fetched from backend), English/Hindi language toggle, category management section, and app info with caffeine.ai attribution footer.",
  "frontend/src/components/AddTransactionModal.tsx": "Add Transaction modal with type toggle (income/expense), amount, account selector, category selector, date, and description fields. Uses shadcn Select with `position=\"popper\"` and `z-[200]` to fix dropdown interaction inside Dialog.",
  "frontend/src/components/TransferMoneyModal.tsx": "Transfer Money modal with independent From/To account selectors, amount, and description fields. Uses shadcn Select with `position=\"popper\"` and `z-[200]` to fix dropdown interaction inside Dialog, with same-account validation.",
  "frontend/src/pages/AccountsPage.tsx": "Accounts page showing all user accounts with balance, account type icon, Transfer and Delete buttons per account. Transfer button opens TransferMoneyModal pre-filled with the selected account; Add Account dialog uses fixed Select dropdowns.",
  "frontend/src/components/BottomNavigation.tsx": "Bottom navigation bar with FAB (floating action button) for adding transactions and nav items for all pages. Fixed `AddTransactionModal` prop from `onClose` to `onOpenChange` to match the updated modal interface."
}